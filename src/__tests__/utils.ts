import { ConnectionCredentials } from '../utils/database/connection-manager'

export const credentials: ConnectionCredentials = {
  url: 'http://localhost:8123',
  username: 'default',
  database: 'test',
  password: '',
}
